# Modo de operação: 'online' ou 'offline'
# offline = usa localStorage (padrão)
# online = conecta com backend Python
VITE_MODE=offline

# URL da API do backend (FastAPI)
VITE_API_URL=http://localhost:8000
